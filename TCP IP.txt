数据链路层的三个基本问题；封装成帧，透明传输，差错检测
封装成帧：根据首部和尾部的标记，从收到的比特流中识别帧的开始和结束，首部和尾部的一个重要作用就是帧定界
链路层规定了最大传输单元-MTU，除去首部和尾部的剩余部分
控制字符SOH表示帧的开始，EOT表示帧的结束
透明传输：传输的数据中不会出现SOH&EOT字符的传输叫做透明传输
若出现SOH或EOT，要在其前面插入转义字符ESC，称其为字节填充或字符填充
差错检测：循环冗余检验CRC
