$$ type fun()const{} 只读函数只能作为类的成员函数，不能修改类的成员变量

@@ 用 new/delete 创建/删除数组
## int *a = new int[100]; delete []a;

@@ 如果重载了默认的复制构造函数，但不进行任何操作会怎样
## 在需要执行复制构造函数时，不进行任何初始化，默认的复制构造函数会进行浅拷贝

@@ 执行深度拷贝构造函数的步骤
## 首先为指针分配一块内存，然后从参数对象指针中拷贝内存，注意形参必须是 const & 